name: Deploy Narada

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        run: |
          cd /opt/stacks/narada
          git pull
          docker compose up -d --build