# Attention layer (not a Firefly clone)

Status: Next

## Goal

Expose what Dhan does not: due SMS, push exceptions, and a mixed timeline. Posted ledger stays in Firefly III.

Dhan owns: transactions, balances, charts, budgets. Narada owns: SMS truth, classify/skip reasons, bills that never post, blocked/unpushed rows, homelab events.

## Task order

1. **Due feed** — `GET /knowledge?kind=due` (or `type=due`) from `sms_analysis` where subcategory `bill` and cashFlow `NEUTRAL`. Include due date, min/total due, last4 when present. These rows never enter `financial_events`.
2. **Push exceptions** — `GET /knowledge?pushed=false` already lists unpushed posted events. Add blocked/skip-opening reasons (dry-run reason on the API, not only CLI logs).
3. **`GET /timeline`** — chronological mix of:
   - due / exception items
   - service events (`narada_events`)
   - optional posted financial (link to Dhan journal id, do not re-skin the ledger)
   Envelope same as `/knowledge`: `{ type, id, occurredAt, payload }`. Filters: `from`, `to`, `type`.
4. **Telegram** — notify new dues and repeated Firefly `blocked` (same change-detection idea as service alerts). No spend summaries (Dhan already reports).
5. **Dashboard (later)** — Home = attention only (dues, exceptions, service strip, last import). No Money charts, net worth, or budgets.
6. **`GET /knowledge/search`** — merchant/body text after timeline exists.
7. **Housekeeping** — close GitHub issues that are already shipped (`#32`–`#38` SMS ingest/classifier/folder watcher).

## Out of scope

- Firefly-style transaction list, reports, or account screens
- RAG, vector DB, AI extraction
- Bank statement importer
- Paperless / Gmail / calendar
- API keys

## Acceptance

- Due reminders are queryable without opening Dhan
- A blocked push is visible over HTTP, not only `sms:firefly-map` stdout
- Timeline is useful with Dhan closed (dues + infra + exceptions)
- No new UI that duplicates Firefly transaction views
